# Что осталось перенести

Прототип `design/evaporate-launcher.html` — спецификация; его разделы
описаны в `design/README.md`. Ниже — то, чего в приложении ещё нет,
и порядок, в котором это стоит делать.

Правило прежнее: переносим по одному связному куску за раз, ошибки
прототипа чиним в обоих местах, каждый новый тест проверяем поломкой.

## Сделано

| Кусок | Коммит |
|---|---|
| Токены, галерея, каркас окна, атмосфера | `ec394ce` |
| Герой библиотеки | `c4fb417` |
| Ритуал запуска | `a74871f` |
| Настоящие начертания шрифтов | `bdb8caa` |
| Стекло навигационного слоя | `2aa7a40` · `49292de` |
| Карточка игры | `733f9eb` |
| Шесть состояний героя | `3fded92` |
| Раздел «Загрузки» | `838cb42` |
| Раздел «Сохранения» | `93c803f` |
| Раздел «Друзья» | `7d73d2f` |
| Раздел «Профиль» | `d08b365` |
| Профиль другого игрока | `55fca1f` |
| Стыки каркаса как в вебе, со стеклом | `22140f9` |
| Полные «Настройки» | `16ac7f8` |
| Первый запуск | `f98e80b` |
| Второй запуск | `f6d4f24` |
| Оверлей в игре | ещё не в коммите |

## Осталось — два куска и сверка

Считая крупными кусками (такими же, как «Загрузки» или «Друзья»):
**два**, плюс финальная сверка. Оба — «Режимы просмотра» и «Звук» —
заметно больше пройденных.

---

### 1. Режимы просмотра — B, C, D

Спецификация — `README.md` → «Режимы просмотра». Переключатель из трёх
иконок в топбаре рядом с хлебной крошкой, виден только на библиотеке.

- **B · Стена** — плотная сетка обложек, фильтры (Все · Установлены ·
  Качаются · Недавние), счётчик. Выделенная игра разворачивается
  **на первой позиции** на 3 × 2 клетки, чтобы сетка не прыгала.
- **D · Терминал** — моноширинная таблица, любой столбец сортирует,
  повторный клик переворачивает порядок, строки ходят `↑` `↓`,
  `Enter` запускает.
- **C · Пульт** — полный экран для дивана и геймпада, карусель из пяти
  обложек, фон из размытой копии выбранной, подсказки по кнопкам
  геймпада. Открывается клавишей `P`, рейл и топбар не нужны.

B и D — виды библиотеки, C забирает весь экран.

---

### 2. Звук

Спецификация — `README.md` → «Звук», каталог с прослушиванием —
раздел 07 в `evaporate-brand.html`.

- Девять голосов, ничего не сэмплировано: числа в таблице README — это
  аргументы в коде. В Dart синтез придётся собрать заново (в прототипе
  Web Audio); кандидаты — генерация PCM на месте и `just_audio`
  или `flutter_soloud`.
- Правила: выключен по умолчанию; звук никогда не громче картинки
  (выключен эффект — выключен и его звук, «Эко» глушит ритуал);
  интерфейс живёт в 200 Гц – 4 кГц, ниже 120 Гц — ритуал; длительности
  по классам 120 / 400 / 2600 мс; пассивные события молчат; шина через
  компрессор −18 дБ, 4 : 1.
- Настройки: общий тумблер, громкость, три класса по отдельности
  и фоновая отдушина. Встают в раздел «Звук» в «Настройках» — там сейчас заглушка.

---

### 3. Финальная сверка

- Пройти таблицу «Состояния направления A» в `README.md` и убедиться,
  что каждое состояние есть в приложении.
- Версия героя: карточка пишет v2.4.1, второй запуск — «2.4.2 обновлена».
- Пока идёт игра, приём ужат до 1 МБ/с, а правая колонка библиотеки
  и «Друзья» читают скорость не из очереди и пишут прежние 592 КБ/с
  и 1.34 МБ/с от друзей.
- Дополнить галерею компонентов всем, что появилось.
- Собрать веб-версию и снять экраны всех разделов на 1280 × 720,
  1440 × 900 и 1900 × 1100.
- Убрать «Разработку» из настроек, когда появится движок (не раньше).

## Как делается каждый кусок

1. Прочитать разметку, стили и скрипт раздела в прототипе и парный
   раздел `design/README.md`.
2. Померить прототип в браузере (`design-web` на 8778) — числа берутся
   оттуда, а не придумываются.
3. Данные — в `lib/data/sample_*.dart`; всё, что можно вывести из
   библиотеки или соседнего раздела, выводится, а не пишется вторым
   числом рядом. На этом правиле пока нашлось пятнадцать расхождений
   в прототипе.
4. Виджеты — в свою папку `lib/<раздел>/`, экран — в `lib/screens/`.
   Общее, что встретилось дважды, сразу переезжает в `lib/widgets/`.
5. Состояния раздела переключаются из «Настройки → Разработка»;
   «нет сети» — состояние окна, а не раздела.
6. Снять экран проверочным тестом с настоящими шрифтами, посмотреть
   глазами.
7. Тесты в `test/<раздел>_test.dart`, каждый проверить поломкой
   (скрипт-мутатор в скретчпаде сессии).
8. `flutter analyze`, `flutter test`, `dart format` только на тронутых
   файлах.
9. Раздел в `design/README.md`: что внутри, таблица состояний, что
   поправлено в прототипе, что найдено по дороге, список тестов.
10. Коммит на английском, тело — почему так, а не что сделано.

## Известные вопросы

- **Направление ириса.** README и бренд-документ говорят «ирис
  закрывает кадр», прототип открывает его из центра. Перенесено так,
  как ведёт себя прототип; решить, что правильно, и починить в обоих
  местах.
- **Разделитель дробей.** Написанные руками числа в прототипе идут
  с точкой («68.4 ГБ»), вычисляемые — с запятой («1,8 ГБ»). В порте
  так же. Стоит выбрать одно.
